// Answer = (A + B +C)/Y

#include <iostream>
#include <cmath>

using namespace std;

int main() {
   float ans, a, b, c, y;

        cout << "Input the value of A: ";
        cin >> a;
        cout << "Input the value of B: ";
        cin >> b;
        cout << "Input the value of C: ";
        cin >> c;
        cout << "Input the value of Y: ";
        cin >> y;

    ans = (a+b+c)/y;
        cout << "\nHere is the answer: " << ans;

    return 0;
       
  return 0; 
  
  }