// ARITHMETIC OPERATORS
// Operators are used to perform operations on variables and values.
// In the example below, we use the + operator to add together two values

#include <iostream>
#include <math.h>
#include <string> 

using namespace std;
int main () {
    int x = 12;
    int y = 8;
    int ourGradeNextSem = x - y;
    cout << "Our grade next sem will be " << ourGradeNextSem; 

  return 0;
}